# JVO site images

Served at `/manus-storage/<filename>`. These are the **real original photos**
from the Manus media export (`webdev-static-assets`), matched to the exact
filenames the site references.

- 9 photos: real JVO originals (DSC*, GOD*, jvo-28).
- `jvo-logo_9104afc1.png`: real JVO logo.
- `CommericalWithBrOll_2b1336d8.mp4`: currently a lightweight venue clip
  (3.6 MB). The real commercial from the export is **298 MB** — too large for
  Git/web as-is; it needs compressing (or hosting on YouTube/Vimeo) before use.
